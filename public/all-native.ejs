<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Horizontal scroll | DragScroll</title>
    <link rel="stylesheet" href="./style.css">
</head>

<body>
    <main class="container">
        <h1>Horizontal & Vertical scroll</h1>

        <div class="drag-scroll drag-scroll-all">
            <div class="content">
                <% for (var i =1; i <=20;  i++ ) { %>
                <ul>
                    <% for (var j =1; j <=20;  j++ ) { %>
                    <li class="item">
                        <a href="https://www.google.com/">
                            <img
                                src="https://images.unsplash.com/photo-1606154427799-2f43b8c81742?ixlib=rb-1.2.1&ixid=MXwxMjA3fDB8MHxlZGl0b3JpYWwtZmVlZHwyNnx8fGVufDB8fHw%3D&auto=format&fit=crop&w=500&q=60" />
                        </a>
                    </li>
                    <% } %>
                </ul>
                <% } %>
            </div>
        </div>
    </main>

    <script>
        window.addEventListener('load', () => {
            const $container = document.querySelector('.drag-scroll')
            const $content = $container.querySelector('.content')
            window.dragSroll = new DragScroll({
                $container: $container,
                $content: $content
            });
        })
    </script>
</body>

</html>